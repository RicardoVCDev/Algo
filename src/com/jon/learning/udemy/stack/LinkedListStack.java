package com.jon.learning.udemy.stack;

import com.jon.learning.udemy.Node;

public class LinkedListStack<T> {
    private Node<T> head;
    private Node<T> tail;
    private int size;

    /**
     * Add a value at the top of the stack
     * Top of the stack is the end (tail) of a collection.
     * 
     * @param value
     */
    private void push(Node<T> value) {

    }
}
